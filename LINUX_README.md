# Установить WSL (один раз)
wsl --install -d Ubuntu

# Зайти в WSL
wsl

# Дальше всё как на обычном Ubuntu
cd /mnt/j/Мои\ ИИ-проекты/universal_terminal\ на\ codacode/terminal_commands_app
chmod +x build_scripts/build_linux.sh
./build_scripts/build_linux.sh# Установить WSL (один раз)
wsl --install -d Ubuntu

# Зайти в WSL
wsl

# Дальше всё как на обычном Ubuntu
cd /mnt/j/Мои\ ИИ-проекты/universal_terminal\ на\ codacode/terminal_commands_app
chmod +x build_scripts/build_linux.sh
./build_scripts/build_linux.sh